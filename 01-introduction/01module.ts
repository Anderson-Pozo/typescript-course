

function addNumbers(x: number, y: number): number {
    return x + y;
}

function addNames(name: string): string {
    return name;
}

console.log(addNumbers(3, 6));

console.log(addNames('Anderson'));